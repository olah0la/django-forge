"""Settings package.

Deliberately empty. Import a specific layer via DJANGO_SETTINGS_MODULE —
`config.settings.development`, `.production` or `.test` — never
`config.settings` itself. Re-exporting a default here would recreate exactly
the implicit fallback M3-02 removed.
"""
