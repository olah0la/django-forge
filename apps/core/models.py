"""Shared model building blocks.

This module is intentionally empty for now. M4-04 adds the abstract base
models (timestamps, primary-key strategy) that other apps inherit from.

Abstract bases belong here rather than in each app, so every derived project
gets the same conventions instead of reinventing them per model.
"""
